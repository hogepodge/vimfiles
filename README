# Installation:

git clone ~/.vim

# Create symlinks to vimrc files

ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc

# Switch to the .vim directory and fetch the submodules

cd ~/.vim
git submodule init
git submodule update
